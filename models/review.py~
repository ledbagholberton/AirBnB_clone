#!/usr/bin/python3
""" class review """
from models.base_model import BaseModel


class Review(BaseModel):
    """ User class """
    def __init__(self, place_id = "", user_id = "", text = ""):
        """ Init """
        self.place_email = email
        self.password = password
        self.first_name = first_name
        self.last_name = last_name
